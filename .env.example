# Mixpanel (required for analytics). Do not commit .env with real token.
# Get your token: mixpanel.com → Project → Project Settings → Project Token
NEXT_PUBLIC_MIXPANEL_TOKEN=your_project_token_here
